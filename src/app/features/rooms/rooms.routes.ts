import { Routes } from '@angular/router';
import { RoomsListComponent } from './pages/list/rooms-list.component';

export const roomsRoutes: Routes = [
  { path: '', component: RoomsListComponent },
];
