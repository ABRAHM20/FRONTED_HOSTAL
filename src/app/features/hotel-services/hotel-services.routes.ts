import { Routes } from '@angular/router';
import { ServicesListComponent } from './pages/list/services-list.component';

export const hotelServicesRoutes: Routes = [
  { path: '', component: ServicesListComponent },
];
