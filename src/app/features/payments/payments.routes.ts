import { Routes } from '@angular/router';
import { PaymentsListComponent } from './pages/list/payments-list.component';

export const paymentsRoutes: Routes = [
  { path: '', component: PaymentsListComponent },
];
