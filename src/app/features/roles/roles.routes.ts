import { Routes } from '@angular/router';

import { RolesListComponent } from './pages/list/roles-list.component';

export const rolesRoutes: Routes = [
  { path: '', component: RolesListComponent },
];
