import { Routes } from '@angular/router';
import { StaysListComponent } from './pages/list/stays-list.component';

export const staysRoutes: Routes = [
  { path: '', component: StaysListComponent },
];
